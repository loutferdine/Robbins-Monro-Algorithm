using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using System.Xml;

namespace Stochastic
{
    class ImportData
    {
        /*
        string fileName = "http://www.ecb.europa.eu/stats/eurofxref/eurofxref-daily.xml";

            XmlTextReader reader = new XmlTextReader(fileName);

            while (reader.Read())
            {
                while (reader.MoveToNextAttribute())
                {
                    if (reader.Name == "currency" && reader.Value == "USD")
                    {
                        reader.MoveToNextAttribute();
                        Console.WriteLine(reader.Value);
                    }
                }
            }
            Console.ReadLine();
         */
    }
}
